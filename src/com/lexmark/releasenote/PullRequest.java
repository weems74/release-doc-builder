package com.lexmark.releasenote;

public class PullRequest {

	/**
	 * Represents a PR in Github
	 * 
	 * Attributes:
	 * 1) Description
	 * 2) Link
	 * 3) Folders (list of folders associated with the PR
	 * 
	 * Behaviors:
	 * 1) GetDescription
	 * 2) GetLink
	 * 3) GetFolders
	 * 4) CheckForFolder -- see if a certain folder is associated with the PR (?)
	 */
	
}
